# Steam 地图本地化覆盖诊断

> 生成时间：2026-07-13T08:27:18.936339+00:00

## 结论

- Steam 原版地图字幕/标题 `.int`：23 个文件、5263 行；catalog 缺 0，build 缺 0。
- 79 个 Steam `.ctm` 中发现 3986 个有效 localized 对象字段。
- 其中地图显式序列化字段 211 项；确定缺口 0 项。
- 其中 1486 个地图实例行虽未单独输出，但实例未序列化覆盖该字段，且相同英文值已由类默认 `.cht` 覆盖。
- 确定的运行时缺口：**0 项**。
- 地图 catalog 共 99 个文件、7839 行；build 缺 0 行。

## 确定缺口

未发现。

## 日文地图索引差异

- 日文同名地图索引：76 个文件、4053 行；另有 3 个 Steam 地图没有日文同名索引。
- 当前 catalog 未逐实例输出 1478 行。
- 这些未输出行均为地图对象未序列化的继承默认提示；运行时由 `engine.cht`、`ctgame.cht` 或 `ctmarkers.cht` 的来源类字段覆盖，因此不属于实际缺字。
- Steam 反向扫描另发现日文索引没有的对象字段；只有上方列出的显式实例覆盖无法由类默认替代。

## 继承风险

- 地图常用字段是标量字符串；唯一 localized 复合字段 `MissionObjectives.MissionObj` 按完整数组 template 输出。
- 未发现 `AButton.Blurred.Text` 一类“派生类部分覆盖结构体叶字段”的地图问题。
- 本报告中的类默认继承与菜单结构体 fanout 不同：前者只在地图实例没有序列化该字段时才视为有效覆盖。

## 范围与限制

- `.ctm` 反向字段扫描覆盖 Steam 原包全部地图；另对账 `subtitles_geo/ras/yyy_*` 和三套 titlecard。
- `.ctm` 通过 UCC 导出的 Level T3D 检查；类声明和默认值来自 Steam 原包完整脚本导出。
- 纯 native 动态拼接文本仍需游戏内烟测，但不会表现为可枚举的 localized 属性缺口。
